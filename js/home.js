const x=App.incidents,open=x.filter(i=>i.status!=="Resuelta");document.querySelector("#stats").innerHTML=[["Abiertas",open.length],["Prioridad alta",open.filter(i=>App.priority(i)>=80).length],["En curso",open.filter(i=>i.status==="En curso").length],["Resueltas",x.filter(i=>i.status==="Resuelta").length]].map(([a,b])=>`<div class="stat"><strong>${b}</strong><span>${a}</span></div>`).join("");document.querySelector("#incident-list").innerHTML=open.sort((a,b)=>App.priority(b)-App.priority(a)).slice(0,4).map(i=>App.card(i)).join("");